<style>
.navbar-default .navbar-nav > li > a {
    color: white;
}
a{
  color: white;
}
.advance_search{
  color: blue;
}
</style>
<nav  style="margin-bottom:0%; background: linear-gradient(to bottom, #4d0066 90%,white 100%); " >
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to '| Home |', welcome_index_path %></li>
        <li><%= link_to '| Advanced Search |', equipment_advance_path %></li>
      <% if current_user.present? %>
        <% @name = current_user.name %>
      <% else %>
        <% @name = ' ' %>
      <% end %>
      <% @u = User.find_by(name: @name) %>
      <% if @u.user_type == 'admin' %>
        <li><%= link_to '| Reserved |', equipment_reserved_path %></li>
        <li><%= link_to '| Audit |', equipment_audit_path %></li>
        <li><%= link_to '| History |', '' %></li>
      <% elsif @u.user_type == 'user' %>
        <li><%= link_to '| Reserved |', equipment_reserved_path %></li>
      <% else %>
      <% end%>
        <!--<li><%= link_to 'About', about_path %></li>-->
        <!--<li><%= link_to 'Contact us', contact_path %></li>-->
      </ul>
    <%= form_tag equipment_search_path , class: "navbar-form navbar-right"%>
    <% if user_signed_in? %>
      <p style="color:white">Welcome, 
      <%= link_to current_user.name, '#' %> (<%= current_user.user_type %>)&nbsp<%= link_to '|', '#' %> &nbsp
      <%= link_to 'Sign out', destroy_user_session_path, method: 'Delete' %>
      </p>
    <% else %>
        <%= link_to 'Log in', user_session_path %>&nbsp;&nbsp;&nbsp<%= link_to '|', '#' %> &nbsp
        <%= link_to 'Sign up', new_user_registration_path %>
    <% end %>
    </div>
  </div>
</nav>
        